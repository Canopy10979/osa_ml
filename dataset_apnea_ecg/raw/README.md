# apnea_ecg — raw source

PhysioNet Apnea-ECG database (WFDB: `.dat` / `.hea` / `.apn` / `.qrs`), ~294 MB.

Record naming carries the label: `a*` = apnea, `b*` = borderline, `c*` = control.
This is the only dataset here with genuine OSA-vs-control labels.

Not tracked in git — see `.gitignore`.
